# Replit-compatible runtime dependencies for SmithAI-Server.
# This excludes the optional llama-cpp-python binary package, which can be
# difficult to build on shared Replit hosts. The server falls back to a
# rule-based responder when no GGUF model is loaded.
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pyyaml>=6.0.1
